server:
  port: 1020
spring:
  application:
   # name: eureka-two #应用名称
eureka:
  client:
    fetch-registry: false #不从注册中心拉取注册表
    register-with-eureka: true #不将自己注册到注册中心
    service-url:
      defaultZone: http://localhost:1010/eureka
